export const SIGN_STEPS = [
	{ step: 1, title: "Preparar" },
	{ step: 2, title: "Resultado" },
] as const;

export const TOTAL_STEPS = 2;

/** Rejilla 3×5 en primera página. */
export const SIG_GRID_COLS = 3;
export const SIG_GRID_ROWS = 5;
